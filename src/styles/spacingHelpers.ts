import {ViewStyle} from 'react-native';

// Padding Horizontal
export const pH16: ViewStyle = {
  paddingHorizontal: 16,
};

// Padding Top
export const pT16: ViewStyle = {
  paddingTop: 16,
};

// Padding Right

// Padding Left

// Padding Bottom
export const pB16: ViewStyle = {
  paddingBottom: 16,
};

// Padding Vertical

// Margin Horizontal

// Margin Vertical

// Margin Top
export const mT16: ViewStyle = {
  marginTop: 16,
};

// Margin Left

// Margin Right

// Margin Bottom
export const mB16: ViewStyle = {
  marginBottom: 16,
};
